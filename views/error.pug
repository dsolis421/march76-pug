extends layout

block content
  main
    section.entry-section
      .entry-blend
        .entry-logo
          h2= message
          h4 Well that's not good
          a(href="/") Try Again
  h4= error.status
  pre #{error.stack}
